# Healing Prompt Notes

## Agent Environment

All agents must use the repository `venv/` virtual environment for Python
dependency installs and tooling. Create it with `python -m venv venv` if missing
and use `venv\\Scripts\\python.exe` / `venv\\Scripts\\pip.exe` for commands. See
`AGENTS.md` for the canonical instructions.
