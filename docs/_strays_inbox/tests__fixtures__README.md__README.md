# tests/fixtures/ - Test Data

Test fixtures for unit and integration tests.

## Subdirectories

- `content/` - Sample markdown content files (blog posts with C# code examples)
- `reference/` - API reference markdown files (for namespace validation tests)

These directories are **read-only** (enforced by `src/core/path_guard.py`).
